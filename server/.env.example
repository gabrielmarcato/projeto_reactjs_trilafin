# Copie para server/.env (fora do git) e ajuste os valores.

# Segredo de assinatura do JWT (troque em produção).
JWT_SECRET=troque-este-segredo

# Usuário inicial criado pelo seed. Se omitidos, usam os padrões do seed.
SEED_USERNAME=gabrielmarcato
SEED_PASSWORD=troque-a-senha

# Porta da API (padrão 3333).
# PORT=3333
